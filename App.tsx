// Expo Router is configured as the entrypoint in package.json ("expo-router/entry").
// This file remains for compatibility with some Expo tooling, but is not used.
export default function App() {
  return null;
}
